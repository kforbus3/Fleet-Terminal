apiVersion: v2
name: fleet-terminal
description: Fleet Terminal — a PAM platform for browser-based SSH (Go backend + React frontend).
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Default application version (overridable via image tags).
appVersion: "0.1.0"
keywords:
  - ssh
  - pam
  - bastion
  - terminal
home: https://github.com/fleet-terminal/fleet-terminal
sources:
  - https://github.com/fleet-terminal/fleet-terminal
maintainers:
  - name: Fleet Terminal
